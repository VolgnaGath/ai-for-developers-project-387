/** AUTOGENERATED - DO NOT EDIT **/
/* tslint:disable */
/* eslint-disable */

import { z } from "zod/v4"

export const s_BadRequest = z.object({ code: z.literal("bad_request") })

export const s_BlankString = z.string().max(254).regex(new RegExp("^\\s*$"))

export const s_BookingStatus = z.literal("confirmed")

export const s_Email = z.email().max(254)

export const s_EventType = z.object({
  id: z.string(),
  title: z.string(),
  description: z.string().optional(),
  durationMinutes: z.union([z.literal(15), z.literal(30)]),
})

export const s_EventTypeDurationLocked = z.object({
  code: z.literal("event_type_duration_locked"),
})

export const s_EventTypeHasBookings = z.object({
  code: z.literal("event_type_has_bookings"),
})

export const s_EventTypeInput = z.object({
  title: z.string().min(1).max(100),
  description: z.string().max(1000).optional(),
  durationMinutes: z.union([z.literal(15), z.literal(30)]),
})

export const s_HealthResponse = z.object({ status: z.literal("ok") })

export const s_InternalError = z.object({ code: z.literal("internal_error") })

export const s_InvalidSlot = z.object({ code: z.literal("invalid_slot") })

export const s_NotFound = z.object({ code: z.literal("not_found") })

export const s_Slot = z.object({
  start: z.iso.datetime({ offset: true }),
  end: z.iso.datetime({ offset: true }),
})

export const s_SlotUnavailable = z.object({
  code: z.literal("slot_unavailable"),
})

export const s_WorkingHours = z.object({
  days: z.array(z.coerce.number()),
  start: z.string(),
  end: z.string(),
})

export const s_Booking = z.object({
  id: z.string(),
  eventTypeId: z.string(),
  guestName: z.string().min(1),
  guestEmail: z.email().optional(),
  start: z.iso.datetime({ offset: true }),
  end: z.iso.datetime({ offset: true }),
  status: s_BookingStatus,
  createdAt: z.iso.datetime({ offset: true }),
})

export const s_BookingInput = z.object({
  eventTypeId: z.string(),
  start: z.iso.datetime({ offset: true }),
  guestName: z.string().min(1).max(100),
  guestEmail: z.union([s_BlankString, s_Email]).optional(),
})

export const s_PublicConfig = z.object({
  timezone: z.string(),
  bookingWindowDays: z.coerce.number(),
  slotStepMinutes: z.coerce.number(),
  workingHours: s_WorkingHours,
})
